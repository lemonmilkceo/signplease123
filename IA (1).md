# 싸인해주세요 (signplease) - Information Architecture

## 📌 서비스 개요

- **서비스명**: 싸인해주세요 (signplease)
- **목적**: AI 기반 아르바이트 표준근로계약서 작성 및 전자서명 서비스
- **타겟 사용자**: 식당/카페 사장님 및 아르바이트생
- **플랫폼**: 모바일 웹 (PWA)

---

## 🗂️ 사이트맵 (Sitemap)

```
싸인해주세요
│
├── 🏠 공통 (Public)
│   ├── / (Splash)
│   ├── /onboarding (서비스 소개)
│   ├── /signup (회원가입)
│   ├── /login (로그인)
│   ├── /forgot-password (비밀번호 찾기)
│   ├── /reset-password (비밀번호 재설정)
│   ├── /select-role (역할 선택 - 사장님/알바생)
│   ├── /terms (이용약관)
│   ├── /privacy (개인정보처리방침)
│   └── /support (고객지원/FAQ)
│
├── 👔 사업자 (Employer)
│   ├── /employer (대시보드)
│   │   ├── 대기 중인 계약
│   │   ├── 완료된 계약
│   │   ├── 폴더 관리
│   │   └── 휴지통
│   ├── /employer/create (계약서 작성)
│   ├── /employer/preview/:id (계약서 미리보기/서명)
│   ├── /employer/contract/:id (계약서 상세)
│   └── /employer/chat (채팅)
│
├── 👷 근로자 (Worker)
│   ├── /worker (대시보드)
│   │   ├── 대기 중인 계약
│   │   ├── 서명 완료된 계약
│   │   ├── 폴더 관리
│   │   └── 휴지통
│   ├── /worker/onboarding (근로자 온보딩)
│   ├── /worker/contract/:id (계약서 확인/서명)
│   ├── /worker/chat (채팅)
│   └── /worker/career (경력 관리)
│
└── 💰 결제/설정
    ├── /pricing (계약서 크레딧 구매)
    ├── /legal-review-pricing (AI 법률 검토 크레딧 구매)
    ├── /bundle-pricing (묶음 상품)
    ├── /profile (프로필 설정)
    └── /payment-history (결제 내역)
```

---

## 🔄 사용자 흐름 (User Flow)

### 1. 신규 사용자 진입

```
Splash → Onboarding → Signup/Login → Select Role → Dashboard
```

### 2. 사업자 계약서 작성 플로우

```
Dashboard
    → Create Contract (Step-by-Step)
        → Step 1: 사업장 규모 선택 (5인 미만/이상)
        → Step 2: 근로자 이름 입력
        → Step 3: 시급 설정
        → Step 4: 근무 시작일
        → Step 5: 근무 요일/주N일 선택
        → Step 6: 근무 시간
        → Step 7: 휴게 시간
        → Step 8: 근무 장소
        → Step 9: 업무 내용
        → Step 10: 급여 지급일
    → AI 계약서 생성
    → Contract Preview
        → 서명
        → 공유 (카카오톡/링크)
        → PDF 다운로드
        → AI 법률 검토
```

### 3. 근로자 계약서 서명 플로우

```
카카오톡/링크 접속
    → Login (필요시)
    → Contract View
        → 계약 조건 확인
        → 어려운 용어 AI 설명
        → 전자서명
    → 완료 (PDF 보관)
```

### 4. 채팅 플로우

```
Dashboard → Chat
    → 채팅방 목록
    → 채팅방 선택/생성
    → 메시지 송수신
    → 파일 첨부
```

---

## 📊 정보 구조 (Data Structure)

### 핵심 엔티티

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│     profiles    │     │    contracts    │     │  chat_rooms     │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│ user_id         │────▶│ employer_id     │◀────│ employer_id     │
│ email           │     │ worker_id       │     │ worker_id       │
│ name            │     │ worker_name     │     │                 │
│ role            │     │ hourly_wage     │     └────────┬────────┘
│ phone           │     │ start_date      │              │
│ bank_info       │     │ work_days       │              ▼
└─────────────────┘     │ work_time       │     ┌─────────────────┐
                        │ status          │     │  chat_messages  │
                        │ signatures      │     ├─────────────────┤
                        └────────┬────────┘     │ room_id         │
                                 │              │ sender_id       │
                                 ▼              │ content         │
                        ┌─────────────────┐     │ file_url        │
                        │ contract_folders│     └─────────────────┘
                        ├─────────────────┤
                        │ user_id         │
                        │ name            │
                        │ color           │
                        └─────────────────┘
```

### 보조 엔티티

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  user_credits   │     │ legal_review_   │     │ worker_reviews  │
│                 │     │ credits         │     │                 │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│ user_id         │     │ user_id         │     │ contract_id     │
│ free_credits    │     │ free_reviews    │     │ employer_id     │
│ paid_credits    │     │ paid_reviews    │     │ worker_id       │
│ total_used      │     │ total_used      │     │ rating          │
└─────────────────┘     └─────────────────┘     │ comment         │
                                                └─────────────────┘

┌─────────────────┐     ┌─────────────────┐
│ contract_       │     │ user_           │
│ invitations     │     │ preferences     │
├─────────────────┤     ├─────────────────┤
│ contract_id     │     │ user_id         │
│ phone           │     │ role            │
│ status          │     │ sort_option     │
│ worker_id       │     │ custom_order    │
└─────────────────┘     └─────────────────┘
```

---

## 🎨 컴포넌트 구조

### 페이지 컴포넌트

```
src/pages/
├── Splash.tsx              # 시작 화면
├── Onboarding.tsx          # 서비스 소개
├── SelectRole.tsx          # 역할 선택
├── Signup.tsx              # 회원가입
├── Login.tsx               # 로그인
├── ForgotPassword.tsx      # 비밀번호 찾기
├── ResetPassword.tsx       # 비밀번호 재설정
├── Profile.tsx             # 프로필 설정
├── Pricing.tsx             # 크레딧 구매
├── LegalReviewPricing.tsx  # 법률검토 크레딧
├── BundlePricing.tsx       # 묶음 상품
├── PaymentHistory.tsx      # 결제 내역
├── Terms.tsx               # 이용약관
├── Privacy.tsx             # 개인정보처리방침
├── Support.tsx             # 고객지원
├── NotFound.tsx            # 404
│
├── employer/
│   ├── Dashboard.tsx       # 사업자 대시보드
│   ├── CreateContract.tsx  # 계약서 작성
│   ├── ContractPreview.tsx # 계약서 미리보기
│   └── Chat.tsx            # 사업자 채팅
│
└── worker/
    ├── Dashboard.tsx       # 근로자 대시보드
    ├── ContractView.tsx    # 계약서 확인
    ├── Career.tsx          # 경력 관리
    └── Chat.tsx            # 근로자 채팅
```

### 공용 컴포넌트

```
src/components/
├── AppDrawer.tsx           # 사이드 메뉴
├── CreditsBadge.tsx        # 크레딧 표시
├── NavLink.tsx             # 네비게이션 링크
├── NoCreditModal.tsx       # 크레딧 부족 모달
├── PDFPreviewModal.tsx     # PDF 미리보기
├── ShareContractModal.tsx  # 계약서 공유
├── SignatureCanvas.tsx     # 전자서명
├── SupportChat.tsx         # AI 고객지원 채팅
├── WorkerReviewModal.tsx   # 근로자 리뷰
├── AllowanceCalculator.tsx # 수당 계산기
│
├── chat/
│   ├── ChatRoomList.tsx    # 채팅방 목록
│   └── ChatView.tsx        # 채팅 화면
│
└── ui/                     # shadcn/ui 컴포넌트
    ├── button.tsx
    ├── card.tsx
    ├── dialog.tsx
    ├── input.tsx
    └── ... (기타 UI 컴포넌트)
```

---

## ⚡ Edge Functions (백엔드)

```
supabase/functions/
├── generate-contract/      # AI 계약서 생성
├── contract-legal-advice/  # AI 법률 검토
├── explain-term/           # AI 용어 설명
├── support-chat/           # AI 고객지원
└── submit-inquiry/         # 문의 접수
```

---

## 🔐 인증 및 권한

### 인증 방식

- Google OAuth 2.0 (Supabase Auth)
- 이메일/비밀번호

### 역할 기반 접근 제어

| 역할              | 접근 가능 영역                                    |
| ----------------- | ------------------------------------------------- |
| 미인증            | Splash, Onboarding, Login, Signup, Terms, Privacy |
| 사업자 (Employer) | /employer/\*, /pricing, /profile, /chat           |
| 근로자 (Worker)   | /worker/\*, /pricing, /profile, /chat             |

### RLS 정책 요약

- 계약서: 본인이 작성한 계약서만 조회/수정 가능
- 채팅: 참여 중인 채팅방만 접근 가능
- 프로필: 본인 프로필만 수정 가능
- 크레딧: 본인 크레딧만 조회/사용 가능

---

## 📱 반응형 설계

- **기본**: 모바일 퍼스트 (max-width: 448px)
- **레이아웃**: 단일 컬럼, 하단 고정 버튼
- **터치**: 최소 터치 영역 44px
- **폰트**: 최소 16px (가독성 확보)

---

## 🎯 핵심 사용자 시나리오

### 시나리오 1: 첫 계약서 작성 (사장님)

1. 앱 접속 → 온보딩 확인
2. 구글 로그인
3. "사장님" 역할 선택
4. "새 계약서 작성" 클릭
5. Step-by-Step 정보 입력
6. AI가 계약서 생성
7. 서명 후 카카오톡으로 공유

### 시나리오 2: 계약서 서명 (알바생)

1. 카카오톡 링크 클릭
2. 구글 로그인 (또는 회원가입)
3. 계약 조건 카드 확인
4. 모르는 용어 터치 → AI 설명
5. 전자서명
6. PDF 저장

### 시나리오 3: 경력 관리 (알바생)

1. 대시보드 → 경력 관리
2. 완료된 계약 목록 확인
3. 경력증명서 다운로드

---

## 📈 버전 히스토리

| 버전 | 날짜    | 주요 변경사항                |
| ---- | ------- | ---------------------------- |
| 1.0  | 2025-01 | 초기 버전 - 계약서 작성/서명 |
| 1.1  | 2025-02 | 채팅 기능 추가               |
| 1.2  | 2025-03 | AI 법률 검토 기능            |
| 1.3  | 2025-04 | 폴더 관리, 휴지통 기능       |

---

## 🔗 관련 문서

- [spec.md](../spec.md) - 서비스 기획서
- [tailwind.config.ts](../tailwind.config.ts) - 디자인 시스템
- [Supabase Schema] - 데이터베이스 스키마
